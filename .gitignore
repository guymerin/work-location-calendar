# Firebase config
# Note: Firebase web config is not secret (it's public in client-side apps).
# Security is enforced via Firestore rules and API key domain restrictions.
# config.js is committed so GitHub Pages can serve it.
firebase-config.js

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Test harness
test/node_modules/
test/screenshots/
test/package-lock.json

