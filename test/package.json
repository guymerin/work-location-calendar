{
  "name": "work-location-calendar-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Headless smoke tests for the Work Location Calendar (mocked Firebase, never touches real data).",
  "scripts": {
    "test": "node modal.test.mjs",
    "test:mobile": "node mobile.test.mjs",
    "postinstall": "playwright install chromium webkit",
    "test:strava": "node strava.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
